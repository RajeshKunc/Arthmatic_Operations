#include <stdio.h>
#include "../Includes/main.h"

int main(void)
{
    int a = 20,b = 20;
    printf(" a = 10 and b = 10 \n");
    addition(a,b);
    subtraction(a,b);
    multiplication(a,b);
    division(a,b);
    return 0;
}

